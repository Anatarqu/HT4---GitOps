# =====================================================================
# Crossplane Composition
# =====================================================================
# Realizado: Composición para instanciar bases de datos PostgreSQL
#
# Guía de referencia:
#   https://docs.crossplane.io/latest/concepts/compositions/
# =====================================================================

apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: postgresql-composition
<<<<<<< HEAD
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  # Referencia exacta al XRD creado en el paso anterior
  compositeTypeRef:
    apiVersion: database.poc.io/v1alpha1
    kind: XPostgreSQLInstance
  
  # Modo de ejecución basado en el pipeline de funciones modernas
  mode: Pipeline
  pipeline:
  - step: patch-and-transform
    functionRef:
      name: function-patch-and-transform
    input:
      apiVersion: pt.fn.crossplane.io/v1beta1
      kind: Resources
      resources:
      - name: postgresql-database
        base:
          # Managed Resource del proveedor tages/provider-postgresql de Upbound
          apiVersion: postgresql.upbound.io/v1beta1
          kind: Database
          spec:
            forProvider:
              # Inicialización explícita requerida por el validador OpenAPI
              name: "placeholder"
            providerConfigRef:
              # Vinculación obligatoria con tu configuración de credenciales
              name: postgresql-config
        patches:
          # Parche para transferir dinámicamente el parámetro dbName del desarrollador
          - type: FromCompositeFieldPath
            fromFieldPath: "spec.dbName"
            toFieldPath: "spec.forProvider.name"
            policy:
              fromFieldPath: Required
=======
  labels:
    # Vincula esta composición al XRD mediante la API correspondiente
    crossplane.io/xrd: xpostgresqlinstances.database.poc.io
spec:
  compositeTypeRef:
    apiVersion: database.poc.io/v1alpha1
    kind: XPostgreSQLInstance
  mode: Pipeline
  pipeline:
    - step: patch-and-transform
      functionRef:
        name: function-patch-and-transform
      input:
        apiVersion: pt.fn.crossplane.io/v1beta1
        kind: Resources
        resources:
          - name: postgresql-database
            base:
              apiVersion: postgresql.postgresql.crossplane.io/v1alpha1
              kind: Database
              spec:
                forProvider:
                  # Valores por defecto requeridos por el esquema del Managed Resource
                  owner: ""
                providerConfigRef:
                  name: postgresql-config
            patches:
              # Patch 1: Pasa el nombre de la base de datos configurada por el dev
              - type: FromCompositeFieldPath
                fromFieldPath: spec.parameters.dbName
                toFieldPath: spec.forProvider.database
                policy:
                  fromFieldPath: Required

              # Patch 2: Pasa el propietario de la base de datos configurada por el dev
              - type: FromCompositeFieldPath
                fromFieldPath: spec.parameters.owner
                toFieldPath: spec.forProvider.owner
                policy:
                  fromFieldPath: Required
>>>>>>> c7a2865 (Desarrollo)
